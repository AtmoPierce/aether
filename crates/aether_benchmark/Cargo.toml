[package]
name = "aether_benchmark"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib"]

[features]
default = ["no_std"]
std = []
no_std = []

[dependencies]
minstant = "0.1.7"
once_cell = "1"
