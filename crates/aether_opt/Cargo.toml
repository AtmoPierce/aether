[package]
name = "aether_opt"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
aether = { path = "../aether" }
aether_rand = { path = "../aether_rand" }
num-traits = "0.2"

[features]
default = ["std"]
std = []
